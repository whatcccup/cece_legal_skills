# NNN-技能英文名（中文名）

> 模板文件：复制本文件到 `NNN-<skill-name>/README.md` 后逐段填写，
> 段落顺序与小标题**不要改**，缺失的段落写「待补」，不要删。

## 1. 一句话定位

**解决谁的什么问题** —— 一句话，不超过两行。写清楚用户角色 + 场景 + 结果。

例：法务 / 律师在把合同发给外部前，一键去掉姓名、账号、金额等敏感信息，
并保留 mapping 以便随时还原成原文。

## 2. 快速开始

给出**推荐安装方式的一句话 Prompt**（用户直接复制发给 AI Agent），再给一条手动命令兜底。

> **（把这句 Prompt 写在这里）**

```bash
# 手动安装命令
```

## 3. 能力边界

| 项 | 内容 |
|---|---|
| 支持 | …… |
| 不支持 | ……（扫描件 PDF、需要 OCR 之类，必写） |
| 已知限制 | …… |
| 合规红线 | ……（数据是否出网、是否替代合规判断，必写） |

## 4. 目录地图

说明本技能文件夹内每个目录装什么（结构固定，见仓库根 README 的《目录规范》）：

| 目录 | 性质 | 内容 |
|---|---|---|
| `01-source/` | 定稿·唯一真源 | 完整可运行工程 |
| `02-github/` | 定稿·发布物料 | CHANGELOG、发布检查清单 |
| `03-skillhub/` | 定稿·发布形态 | SKILL.md + scripts + references |
| `90-build/` | 构建产物 | 构建脚本与 zip（zip 不入库） |
| `99-temp/` | 临时 | 会话 / 报告 / 样本（不入库） |

## 5. 维护说明

- **改代码**：只改 `01-source/`，跑 `90-build/build.sh` 同步到 `03-skillhub/`
- **自检**：……（具体命令）
- **发 GitHub 版**：commit + push
- **发 SkillHub 版**：改 `03-skillhub/SKILL.md` 的 `version`（SemVer）→ `skillhub publish`
- **禁止**：直接手改 `03-skillhub/scripts/` 下的源码（会被构建脚本覆盖）

## 6. 许可与来源

MIT © 2026 whatcccup ｜ 源码：<仓库地址> ｜ SkillHub：<技能页地址>
